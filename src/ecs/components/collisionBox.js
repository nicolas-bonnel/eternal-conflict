var collisionBox = {
	name: 'collisionBox',
	//type is rectangle or elipse
	init: function (x, y, z, type) {
        this.x = x;
        this.y = y;
        this.z = z;
        this.type = type;
    }
};