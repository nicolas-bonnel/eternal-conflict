var AnimatedMeshComponent = CES.Component.extend(animatedMesh);
var CollisionBoxComponent = CES.Component.extend(collisionBox);
var InventoryComponent = CES.Component.extend(inventory);
var PositionComponent = CES.Component.extend(position);
var RotationComponent = CES.Component.extend(rotation);
var StaticMeshComponent = CES.Component.extend(staticMesh);
var VelocityComponent = CES.Component.extend(velocity);
var WaterMeshComponent = CES.Component.extend(waterMesh);
